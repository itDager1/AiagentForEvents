# 🤖 AI Парсер Мероприятий - Документация

## Обзор

Система автоматического извлечения информации о мероприятиях с официальных сайтов с использованием искусственного интеллекта (OpenRouter API + GPT-4o Mini).

## 🎯 Основная цель

**Получение ТОЧНЫХ и АКТУАЛЬНЫХ дат мероприятий** с официальных источников, чтобы информация в приложении всегда соответствовала реальным данным.

## 🔍 Как работает извлечение дат

### Приоритеты источников данных

1. **HTML элементы `<time datetime>`** (Самый надежный)
   - Стандартный способ разметки дат в HTML5
   - ISO формат: `<time datetime="2025-11-27T10:00:00">27 ноября 2025</time>`

2. **JSON-LD структурированные данные** (schema.org)
   - Микроразметка Event schema
   - Поля: `startDate`, `endDate`, `eventSchedule`

3. **CSS классы**
   - `.date`, `.event-date`, `.schedule-date`
   - `[class*="date"]`, `[class*="Date"]`
   - `[itemprop="startDate"]`, `[itemprop="endDate"]`

4. **Текстовые паттерны**
   - "27-28 ноября 2025"
   - "November 27-28, 2025"
   - "15 декабря 2025, 18:00"

## 📋 Процесс работы

### 1. Получение контента страницы

```typescript
// Используется CORS proxy для обхода ограничений браузера
const html = await fetchWebpageContent(url);
```

### 2. Очистка и структурирование HTML

```typescript
const cleanedContent = cleanHtmlContent(html);
```

Извлекаются:
- Заголовки (h1, h2, h3)
- Мета-описание
- **Все элементы `<time>` с атрибутами datetime**
- **Блоки с датами** (по CSS селекторам)
- **JSON-LD данные** (structured data)
- Основной контент страницы

### 3. AI анализ с помощью OpenRouter

```typescript
const response = await fetch('https://openrouter.ai/api/v1/chat/completions', {
  model: 'openai/gpt-4o-mini',
  temperature: 0.1, // Очень низкая для точности
  // ...
});
```

**Промпт для AI:**
- Текущая дата (для проверки актуальности)
- Структурированное содержимое страницы
- Четкие инструкции по извлечению дат
- Примеры правильного формата
- Требование возврата JSON или null

### 4. Валидация извлеченных данных

```typescript
// Проверка 1: Дата в валидном ISO формате
const eventDate = new Date(eventData.date);
if (isNaN(eventDate.getTime())) return null;

// Проверка 2: Дата в будущем
if (eventDate < new Date()) return null;

// Проверка 3: Год разумный (текущий год ± 2 года)
if (eventYear < currentYear || eventYear > currentYear + 2) return null;
```

## 🛠️ Использование

### В админ-панели

1. Войдите как администратор (пароль: `123456`)
2. Перейдите в "Панель администратора"
3. Выберите вкладку "Сбор мероприятий (AI)"
4. Введите URL официального сайта (например: `https://highload.ru`)
5. Нажмите "Извлечь данные"
6. Проверьте результат и добавьте мероприятие

### Пакетная обработка

Можно обработать сразу несколько URL:

```
https://highload.ru
https://holyjs.ru
https://heisenbug.ru
https://jokerconf.com
```

AI обработает их последовательно с задержкой 2 секунды между запросами.

## 📊 Извлекаемые данные

```typescript
interface Event {
  title: string;              // Официальное название
  description: string;        // Описание (2-3 предложения)
  date: string;               // ISO: "2025-11-27T10:00:00"
  displayDate?: string;       // "27–28 ноября 2025"
  format: EventFormat;        // Онлайн | Оффлайн | Гибрид
  category: EventCategory;    // Конференция | Хакатон | Митап | Обучение
  location: string;           // Место проведения
  tags: string[];             // Релевантные теги
  image: string;              // Автоматически из Unsplash
  originalLink: string;       // URL источника
}
```

## ✅ Преимущества

1. **Точность дат** - AI извлекает даты из официальных источников
2. **Актуальность** - Автоматическая проверка на актуальность
3. **Автоматизация** - Не нужно вручную вводить все данные
4. **Пакетная обработка** - Можно добавить много событий сразу
5. **Валидация** - Множественные проверки корректности данных

## ⚠️ Ограничения

1. **CORS** - Некоторые сайты могут блокировать запросы (используется proxy)
2. **Структура сайтов** - Работает лучше на стандартизированных сайтах
3. **API лимиты** - OpenRouter имеет ограничения на количество запросов
4. **Стоимость** - Каждый запрос к OpenRouter API платный

## 🔧 Технические детали

### Модель AI
- **OpenAI GPT-4o Mini** через OpenRouter
- Temperature: 0.1 (для максимальной точности)
- Max tokens: 1500

### API Key
Хранится в Supabase Vault:
```sql
SELECT vault.get_secret('OPENROUTER_API_KEY');
```

### Задержки
- Между запросами: 2 секунды (rate limiting)
- Таймаут fetch: стандартный браузерный

## 📝 Примеры использования

### Пример 1: Одно мероприятие
```
URL: https://highload.ru
→ AI извлекает: HighLoad++ 2025, 27–28 ноября, Москва
```

### Пример 2: Поиск по названию
```
Запрос: "HolyJS 2025"
→ AI находит официальный сайт и актуальные даты
```

### Пример 3: Пакетная загрузка
```
https://highload.ru
https://holyjs.ru
https://heisenbug.ru
→ AI обрабатывает все 3 мероприятия
```

## 🚀 Будущие улучшения

1. **Кэширование** - Сохранение результатов для избежания повторных запросов
2. **Планировщик** - Автоматическое обновление дат раз в неделю
3. **Больше источников** - Поддержка большего количества форматов сайтов
4. **Уведомления** - Оповещения об изменении дат
5. **История** - Логирование всех изменений дат

## 📞 Поддержка

При проблемах с парсингом:
1. Проверьте консоль браузера (F12)
2. Убедитесь, что API ключ настроен
3. Попробуйте другой URL
4. Проверьте, что сайт доступен

---

**Создано для проекта Exact Direction**  
**AI-powered event management for Sber employees**
