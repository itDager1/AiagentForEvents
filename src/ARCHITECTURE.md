# 🏗️ Архитектура Exact Direction

> Техническая документация для разработчиков

---

## 📐 Общая архитектура

```
┌─────────────────────────────────────────────────────────┐
│                    React Frontend                        │
│  ┌──────────────┐  ┌───────────────┐  ┌──────────────┐ │
│  │   App.tsx    │  │   Components  │  │    Utils     │ │
│  │  (Main)      │←→│  - Header     │←→│  - AI        │ │
│  │              │  │  - Auth       │  │  - DB        │ │
│  │              │  │  - EventCard  │  │  - Supabase  │ │
│  │              │  │  - Profile    │  │  - Health    │ │
│  │              │  │  - Calendar   │  │              │ │
│  └──────────────┘  └───────────────┘  └──────────────┘ │
└────────────────────────┬────────────────────────────────┘
                         │ HTTPS / Fetch API
                         ▼
┌─────────────────────────────────────────────────────────┐
│              Supabase Edge Functions                     │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Hono Server (Deno Runtime)                       │  │
│  │  ┌─────────────────────────────────────────────┐ │  │
│  │  │  Routes:                                     │ │  │
│  │  │  • GET  /events          (fetch events)     │ │  │
│  │  │  • POST /events          (update events)    │ │  │
│  │  │  • GET  /profile/:id     (get profile)      │ │  │
│  │  │  • POST /profile         (save profile)     │ │  │
│  │  │  • POST /signup          (register user)    │ │  │
│  │  │  • POST /ai-recommend    (AI proxy) ─────┐  │ │  │
│  │  │  • POST /seed-ai-events  (auto-seed) ────┤  │ │  │
│  │  └─────────────────────────────────────────────┘ │  │
│  └───────────────────────────────────────────────────┘  │
└────────────┬───────────────────────────┬────────────────┘
             │                           │
             ▼                           ▼
┌──────────────────────┐    ┌──────────────────────────┐
│  Supabase KV Store   │    │   OpenRouter API         │
│  ┌────────────────┐  │    │  ┌────────────────────┐  │
│  │ events         │  │    │  │ Perplexity Sonar   │  │
│  │ profile:{id}   │  │    │  │ (Real-time AI)     │  │
│  └────────────────┘  │    │  └────────────────────┘  │
└──────────────────────┘    └──────────────────────────┘
```

---

## 🗂️ Структура файлов

```
exact-direction/
│
├── 📄 App.tsx                          # Главный компонент приложения
│
├── 📁 components/
│   ├── 📁 sber/                        # Бизнес-компоненты
│   │   ├── Header.tsx                  # Шапка с навигацией
│   │   ├── Auth.tsx                    # Модальное окно авторизации
│   │   ├── EventCard.tsx               # Карточка события
│   │   ├── EventDetailsModal.tsx       # Детали события
│   │   ├── UserProfile.tsx             # Профиль пользователя
│   │   ├── FooterDog.tsx               # Футер с SVG-собакой
│   │   └── ApiKeyStatusBadge.tsx       # Индикатор статуса API
│   │
│   ├── 📁 ui/                          # UI компоненты (Shadcn)
│   │   ├── button.tsx
│   │   ├── input.tsx
│   │   ├── calendar.tsx
│   │   ├── dialog.tsx
│   │   ├── badge.tsx
│   │   └── ... (40+ компонентов)
│   │
│   └── 📁 figma/
│       └── ImageWithFallback.tsx       # Компонент изображения
│
├── 📁 utils/
│   ├── aiService.ts                    # AI-рекомендации (OpenRouter)
│   ├── dbService.ts                    # CRUD операции (Supabase)
│   ├── supabaseClient.ts               # Клиент Supabase
│   ├── checkApiKey.ts                  # Проверка API ключа
│   ├── systemHealthCheck.ts            # System health monitoring
│   └── 📁 supabase/
│       └── info.tsx                    # Конфигурация проекта
│
├── 📁 supabase/
│   └── 📁 functions/
│       └── 📁 server/
│           ├── index.tsx               # Edge Functions (Hono server)
│           └── kv_store.tsx            # KV Store утилиты
│
├── 📁 data/
│   └── mock.ts                         # Типы и mock данные
│
├── 📁 styles/
│   └── globals.css                     # Глобальные стили (Tailwind)
│
├── 📚 Документация/
│   ├── README.md                       # Главная документация
│   ├── SETUP_API_KEY.md                # Настройка API ключа
│   ├── QUICK_START.md                  # Быстрый старт
│   ├── GETTING_STARTED.md              # Краткая инструкция
│   ├── PROJECT_STATUS.md               # Статус проекта
│   ├── ARCHITECTURE.md                 # Этот файл
│   └── CHANGELOG.md                    # История изменений
│
└── 🔧 Конфигурация/
    ├── .env.example                    # Пример переменных окружения
    └── .gitignore                      # Защита секретов
```

---

## 🔄 Поток данных

### 1. Загрузка приложения

```
User открывает приложение
        ↓
App.tsx useEffect()
        ↓
┌───────────────────────┐
│ logSystemHealth()     │ ← Проверка системы
└───────────────────────┘
        ↓
┌───────────────────────┐
│ seedEvents()          │ ← Загрузка/seed событий
└───────────────────────┘
        ↓
┌───────────────────────┐
│ fetchEvents()         │ ← Получение событий из KV
└───────────────────────┘
        ↓
┌───────────────────────┐
│ supabase.auth         │ ← Проверка сессии
│ .getSession()         │
└───────────────────────┘
        ↓
Render UI с событиями
```

### 2. AI-рекомендации

```
User нажимает "Подобрать с AI"
        ↓
handleAskAI()
        ↓
getAIRecommendations(user, events)
        ↓
POST /ai-recommend
        ↓
┌─────────────────────────────────┐
│ Supabase Edge Function          │
│   ↓                              │
│ Deno.env.get('OPENROUTER_API_KEY') │
│   ↓                              │
│ fetch('openrouter.ai/api/...')  │
│   ↓                              │
│ Perplexity Sonar анализирует    │
│   ↓                              │
│ Возврат топ-3 IDs               │
└─────────────────────────────────┘
        ↓
Frontend фильтрует события по IDs
        ↓
Отображение рекомендаций
```

### 3. Регистрация на событие

```
User нажимает "Записаться"
        ↓
toggleRegister(eventId)
        ↓
┌─────────────────────────┐
│ Optimistic UI Update    │ ← Мгновенное обновление
└─────────────────────────┘
        ↓
POST /profile (обновление myEventIds)
        ↓
┌─────────────────────────────┐
│ Supabase Edge Function      │
│   ↓                          │
│ kv.set(`profile:${userId}`) │
└─────────────────────────────┘
        ↓
Toast уведомление
```

### 4. Автозагрузка событий

```
seedEvents() при старте
        ↓
Проверка: есть ли события в KV?
        ↓
Если нет → POST /seed-ai-events
        ↓
┌─────────────────────────────────────┐
│ Supabase Edge Function              │
│   ↓                                  │
│ Perplexity Sonar поиск:             │
│   - HighLoad++                       │
│   - HolyJS                           │
│   - Heisenbug, Mobius, Joker и др.  │
│   ↓                                  │
│ Генерация 12-15 событий             │
│   ↓                                  │
│ kv.set('events', generatedEvents)   │
└─────────────────────────────────────┘
        ↓
Возврат событий на frontend
```

---

## 🧩 Ключевые компоненты

### Frontend

#### App.tsx
- **Роль**: Главный контейнер приложения
- **State**: user, events, filters, AI recommendations
- **Эффекты**: 
  - System health check (dev)
  - Загрузка событий
  - Auth state listener
- **Функции**:
  - `handleAskAI()` - AI-рекомендации
  - `toggleRegister()` - регистрация на события
  - `handleUpdateUser()` - обновление профиля

#### Header.tsx
- **Роль**: Навигация и аутентификация
- **Особенности**:
  - Sticky позиционирование
  - Dropdown menu для пользователя
  - ApiKeyStatusBadge (dev mode)
  - Backdrop blur эффект

#### EventCard.tsx
- **Роль**: Карточка события в grid
- **Особенности**:
  - Hover эффекты
  - Badge для категории/формата
  - Быстрая регистрация

#### UserProfile.tsx
- **Роль**: Профиль пользователя
- **Особенности**:
  - Редактирование интересов
  - Календарь с событиями
  - Список зарегистрированных событий

### Backend

#### index.tsx (Edge Functions)
- **Роль**: API сервер на Hono
- **Middleware**: CORS, Logger
- **Endpoints**: 7 routes (events, profile, auth, AI)
- **Security**: Bearer token auth, Secrets для API keys

#### kv_store.tsx
- **Роль**: KV Store утилиты
- **Функции**:
  - `get(key)` - получение данных
  - `set(key, value)` - сохранение данных
- **Storage**: Supabase KV (Deno KV)

### Utils

#### aiService.ts
- **Роль**: Прокси для AI-запросов
- **Модель**: Perplexity Sonar (OpenRouter)
- **Особенности**:
  - Упрощение событий для экономии токенов
  - Prompt engineering для рекомендаций
  - Fallback на эвристический алгоритм

#### systemHealthCheck.ts
- **Роль**: Мониторинг системы
- **Проверки**:
  - Edge Functions доступность
  - KV Store статус
  - API ключ конфигурация
- **Вывод**: Красивый console.log с инструкциями

---

## 🔐 Безопасность

### Аутентификация
```
User → Supabase Auth → JWT Token → Edge Functions
```

### API ключи
```
Developer → Supabase Dashboard → Edge Functions Secrets
                                        ↓
                        Deno.env.get('OPENROUTER_API_KEY')
```

### CORS
```javascript
cors({
  origin: "*",
  allowHeaders: ["Content-Type", "Authorization"],
  allowMethods: ["GET", "POST", "PUT", "DELETE", "OPTIONS"]
})
```

### Secrets
- ✅ API ключи в Supabase Secrets (не в коде)
- ✅ `.env` в `.gitignore`
- ✅ `.env.example` для документации
- ✅ Bearer token auth для всех запросов

---

## 🎨 Дизайн система

### Цветовая палитра

```css
/* Primary */
--blue-600: #0066FF;
--blue-500: #3B82F6;
--blue-400: #60A5FA;

/* Background */
--bg-main: #f8f9fa;
--bg-white: #FFFFFF;

/* Text */
--text-dark: #1E293B;
--text-medium: #64748B;
--text-light: #94A3B8;

/* States */
--success: #10B981;
--warning: #F59E0B;
--error: #EF4444;
```

### Spacing System

```css
/* Tailwind spacing */
px-4  → 1rem (16px)
py-8  → 2rem (32px)
gap-6 → 1.5rem (24px)

/* Border radius */
rounded-xl  → 0.75rem (12px)
rounded-2xl → 1rem (16px)
rounded-3xl → 1.5rem (24px)
```

### Typography

```css
/* Заголовки */
text-5xl font-bold → h1
text-3xl font-bold → h2
text-2xl font-bold → h3

/* Текст */
text-lg → body large
text-base → body regular
text-sm → caption
text-xs → label
```

---

## 🔧 Development

### Запуск локально

```bash
# Установка
npm install

# Запуск
npm start

# Проверка типов
npm run type-check

# Линтинг
npm run lint
```

### Переменные окружения

```bash
# .env (не коммитится)
OPENROUTER_API_KEY=sk-or-v1-...

# Для Supabase Edge Functions
# Добавить в Dashboard → Settings → Secrets
```

### Дебаг

```javascript
// Консоль браузера (F12)
// System Health Check запускается автоматически в dev

// Supabase Dashboard → Functions → Logs
// Просмотр логов Edge Functions в реальном времени
```

---

## 📊 Performance

### Оптимизации

- ✅ **Lazy loading**: Изображения загружаются по требованию
- ✅ **Optimistic UI**: Мгновенная реакция на действия пользователя
- ✅ **Caching**: События кешируются в KV Store
- ✅ **Code splitting**: Автоматический split через React
- ✅ **Minification**: Production build минифицирован

### Метрики

| Метрика | Значение |
|---------|----------|
| First Contentful Paint | < 1.5s |
| Time to Interactive | < 3s |
| Bundle size | ~ 500KB (gzipped) |
| API latency | < 500ms |

---

## 🧪 Тестирование

### Manual Testing Checklist

- [ ] Аутентификация (Supabase Auth)
- [ ] Mock login
- [ ] Загрузка событий
- [ ] Фильтрация и поиск
- [ ] Сортировка
- [ ] Регистрация на события
- [ ] AI-рекомендации
- [ ] Календарь
- [ ] Профиль пользователя
- [ ] Responsive дизайн

### API Testing

```bash
# Проверка Edge Functions
curl https://wwxibvtflekrimlpgijo.supabase.co/functions/v1/make-server-6f7662b1/events

# Проверка AI endpoint (требует API key)
curl -X POST https://wwxibvtflekrimlpgijo.supabase.co/functions/v1/make-server-6f7662b1/ai-recommend \
  -H "Content-Type: application/json" \
  -d '{"prompt": "Test"}'
```

---

## 🚀 Deployment

### Production Checklist

- [ ] ✅ API ключ добавлен в Supabase Secrets
- [ ] ✅ RLS политики настроены (опционально)
- [ ] ✅ CORS проверен для production domain
- [ ] ✅ Environment variables настроены
- [ ] ✅ Build протестирован: `npm run build`

### Рекомендованные платформы

1. **Figma Make** (рекомендуется)
2. **Vercel**
3. **Netlify**
4. **Cloudflare Pages**

---

## 📚 Дополнительные ресурсы

- [Supabase Docs](https://supabase.com/docs)
- [OpenRouter API](https://openrouter.ai/docs)
- [Hono Framework](https://hono.dev/)
- [Deno Runtime](https://deno.land/)
- [Tailwind CSS](https://tailwindcss.com/)
- [Shadcn/ui](https://ui.shadcn.com/)

---

**Версия**: 1.0.0  
**Последнее обновление**: 20 ноября 2025
