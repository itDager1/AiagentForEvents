# 🔑 Настройка OpenRouter API ключа в Supabase

## Проблема

При запуске приложения вы можете увидеть следующие ошибки:
- ❌ `API Error 500 on /seed-ai-events`
- ❌ `Error while deploying: XHR for "/api/integrations/supabase/.../deploy" failed with status 403`

Эти ошибки означают, что **OpenRouter API ключ не настроен** в секретах Supabase Edge Functions.

---

## ✅ Решение: Настройка API ключа

### Шаг 1: Получите API ключ OpenRouter

Если у вас уже есть API ключ OpenRouter, переходите к Шагу 2.

Если нет:
1. Перейдите на [https://openrouter.ai/](https://openrouter.ai/)
2. Зарегистрируйтесь или войдите
3. Перейдите в раздел **API Keys**
4. Создайте новый ключ
5. Скопируйте ключ (он будет выглядеть примерно так: `sk-or-v1-...`)

---

### Шаг 2: Добавьте ключ в Supabase Edge Functions Secrets

1. **Откройте Supabase Dashboard:**
   ```
   https://supabase.com/dashboard/project/wwxibvtflekrimlpgijo/settings/functions
   ```

2. **Перейдите в раздел "Edge Functions":**
   - В левом меню нажмите **Edge Functions**
   - Затем выберите вкладку **Secrets**

3. **Добавьте новый секрет:**
   - Нажмите кнопку **"Add new secret"**
   - **Name:** `OPENROUTER_API_KEY`
   - **Value:** Вставьте ваш API ключ OpenRouter (например, `sk-or-v1-...`)
   - Нажмите **"Add secret"**

4. **Сохраните изменения:**
   - Убедитесь, что секрет сохранен
   - Может потребоваться перезапуск Edge Function

---

### Шаг 3: Перезапустите приложение

После добавления секрета:

1. **Обновите страницу** приложения (F5 или Ctrl+R)
2. **Проверьте консоль браузера** - должны появиться сообщения:
   ```
   ✅ Events already exist in database, skipping seed
   ```
   или
   ```
   ✅ Successfully seeded DB with AI events
   ```

---

## 🔍 Проверка статуса

### Вариант 1: Автоматическая проверка

Приложение автоматически проверяет статус API ключа при запуске. Если ключ не настроен, вы увидите:

```
⚠️ API Key Warning
OPENROUTER_API_KEY is not configured in Supabase secrets
```

### Вариант 2: Ручная проверка

В консоли браузера выполните:

```javascript
// Проверка статуса API ключа
const checkStatus = async () => {
  const response = await fetch(
    'https://wwxibvtflekrimlpgijo.supabase.co/functions/v1/make-server-6f7662b1/seed-ai-events',
    {
      method: 'POST',
      headers: {
        'Authorization': 'Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...'
      }
    }
  );
  const data = await response.json();
  console.log(data);
};
checkStatus();
```

**Ожидаемый результат:**
- ✅ Если ключ настроен: `{ success: true, data: [...] }`
- ❌ Если ключ не настроен: `{ success: false, error: 'OPENROUTER_API_KEY not configured' }`

---

## 🛠️ Troubleshooting

### Проблема: "403 Forbidden" при деплое

**Причина:** Нет прав доступа для деплоя Edge Function.

**Решение:**
1. Убедитесь, что вы являетесь владельцем или администратором проекта Supabase
2. Проверьте, что проект активен и не заморожен
3. Попробуйте обновить страницу Supabase Dashboard

### Проблема: "500 Internal Server Error" на `/seed-ai-events`

**Причина:** API ключ не настроен или настроен неправильно.

**Решение:**
1. Проверьте, что секрет `OPENROUTER_API_KEY` существует в Supabase
2. Убедитесь, что ключ скопирован полностью (включая `sk-or-v1-`)
3. Проверьте, что в ключе нет лишних пробелов
4. Попробуйте удалить и добавить секрет заново

### Проблема: Edge Function не может подключиться к OpenRouter

**Причина:** Неверный API ключ или проблемы с сетью.

**Решение:**
1. Проверьте баланс на аккаунте OpenRouter
2. Убедитесь, что API ключ активен
3. Попробуйте создать новый ключ

---

## 📝 Дополнительная информация

### Как работает AI-функционал

1. **При первом запуске** приложение пытается загрузить актуальные IT-события через OpenRouter API
2. **Если API ключ настроен**, приложение получает список реальных событий (HighLoad++, HolyJS, и т.д.)
3. **Если API ключ не настроен**, приложение использует mock-данные (демонстрационные события)

### Стоимость использования OpenRouter

- OpenRouter использует модель `perplexity/sonar` для поиска событий
- Примерная стоимость: ~$0.001-0.005 за запрос
- Приложение делает запрос только **один раз при первой загрузке** (если база данных пуста)
- Последующие запуски используют кэшированные данные

### Альтернативный вариант без API ключа

Если вы не хотите использовать OpenRouter API, приложение будет работать с mock-данными:

```javascript
// В /utils/dbService.ts автоматически используется fallback:
if (aiResult && aiResult.error) {
  console.warn("⚠️ AI seeding error:", aiResult.error);
  // Fallback to mock events
  await fetchAPI('/events', 'POST', MOCK_EVENTS);
}
```

---

## ✅ Готово!

После настройки API ключа приложение будет автоматически загружать актуальные IT-события из интернета. 

Если у вас возникли проблемы, проверьте:
- [ ] API ключ скопирован полностью
- [ ] Секрет добавлен с правильным именем `OPENROUTER_API_KEY`
- [ ] Приложение перезапущено после добавления секрета
- [ ] Консоль браузера показывает успешную загрузку событий

**Приятного использования!** 🎉
