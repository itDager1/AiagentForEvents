# 🧪 Тестирование AI-функций

**Статус**: ✅ API ключ настроен и работает  
**Дата**: 20 ноября 2025

---

## 🎯 Быстрая проверка

### 1. Запустите приложение

```bash
npm start
```

### 2. Проверьте консоль браузера

При запуске в консоли должно появиться:

```
🔧 Проверка конфигурации API...

✅ API ключ OpenRouter настроен корректно

📚 Документация: см. файл SETUP_API_KEY.md
```

### 3. Проверьте Header (dev mode)

В правом верхнем углу Header должен быть зелёный badge:

🟢 **API ключ настроен**

---

## 🤖 Тестирование AI-рекомендаций

### Шаг 1: Войдите в систему

Используйте **Mock режим** для быстрого тестирования:

1. Нажмите **"Войти"** в Header
2. Выберите **"Использовать демо-аккаунт"**
3. Выберите роль (например, "Разработчик")
4. Нажмите **"Войти как {Роль}"**

### Шаг 2: Запросите рекомендации

1. На главной странице найдите секцию **"AI-АГЕНТ"**
2. Нажмите кнопку **"Подобрать с AI"**
3. Дождитесь загрузки (кнопка покажет "AI анализирует...")

### Шаг 3: Проверьте результат

**✅ Успешный результат:**
- Появляется секция **"Персональные рекомендации"**
- Отображается 2-3 события
- Toast уведомление: "ИИ подобрал для вас мероприятия!"

**❌ Ошибка:**
- Toast уведомление: "Ошибка при обращении к ИИ"
- Проверьте консоль браузера для деталей

---

## 🔄 Тестирование автозагрузки событий

### Автоматическая загрузка

При первом запуске приложение автоматически:

1. Проверяет наличие событий в базе
2. Если база пустая - запрашивает AI для загрузки актуальных событий
3. Загружает 12-15 реальных IT-событий (HighLoad++, HolyJS и др.)

### Ручная проверка

Откройте консоль браузера и проверьте Network вкладку:

1. Найдите запрос к `/seed-ai-events`
2. Проверьте Response:
   ```json
   {
     "success": true,
     "data": [...]
   }
   ```

---

## 🔍 Диагностика проблем

### Проблема: "API ключ не настроен"

**Симптомы:**
- Жёлтый/красный badge в Header
- Ошибка в консоли: "OPENROUTER_API_KEY is not set"

**Решение:**
1. Откройте [Supabase Dashboard](https://supabase.com/dashboard)
2. Проект: `wwxibvtflekrimlpgijo`
3. Settings → Edge Functions → Secrets
4. Проверьте наличие секрета `OPENROUTER_API_KEY`
5. Если отсутствует - добавьте (см. [SETUP_API_KEY.md](./SETUP_API_KEY.md))

### Проблема: AI возвращает пустой массив

**Симптомы:**
- Запрос успешен, но событий нет
- Консоль: "AI Recommendation Error" или "Failed to parse"

**Возможные причины:**
1. **Модель недоступна** - Perplexity Sonar временно недоступна
2. **Неверный формат ответа** - AI вернула некорректный JSON
3. **Нет подходящих событий** - все события отфильтрованы

**Решение:**
- Приложение автоматически использует **Fallback алгоритм**
- Fallback возвращает события на основе эвристических правил
- Проверьте логи в Supabase Dashboard для деталей

### Проблема: Ошибка 429 (Rate Limit)

**Симптомы:**
- Ошибка: "OpenRouter API Error: 429"
- Слишком много запросов к API

**Решение:**
1. Проверьте баланс на [OpenRouter Dashboard](https://openrouter.ai/dashboard)
2. Подождите несколько минут
3. Убедитесь, что приложение не делает избыточные запросы
4. Используйте Fallback алгоритм временно

---

## 📊 Логи и мониторинг

### Консоль браузера

**Dev mode:**
```javascript
// Автоматическая проверка при старте
🔧 Проверка конфигурации API...
✅ API ключ OpenRouter настроен корректно

// При запросе AI-рекомендаций
POST /ai-recommend 200 OK (3.2s)

// При ошибке
AI Recommendation Error: {...}
```

### Supabase Dashboard

1. Откройте [Supabase Dashboard](https://supabase.com/dashboard)
2. Выберите проект `wwxibvtflekrimlpgijo`
3. Functions → Logs
4. Фильтр: `/ai-recommend` или `/seed-ai-events`

**Успешный запрос:**
```
POST /ai-recommend
Status: 200
Response: {"choices": [...]}
```

**Ошибка:**
```
POST /ai-recommend
Status: 500
Error: OPENROUTER_API_KEY is not set
```

---

## 🎮 Тестовые сценарии

### Сценарий 1: Разработчик с интересом к Backend

1. Войти как "Разработчик"
2. В профиле добавить интересы: "Backend", "Java", "Architecture"
3. График работы: "5/2 (Офис)"
4. Запросить AI-рекомендации
5. **Ожидаемо**: События с тегами Backend, Java, в будние дни/вечер

### Сценарий 2: Менеджер с гибким графиком

1. Войти как "Менеджер"
2. Интересы: "Management", "Leadership", "Agile"
3. График работы: "Remote"
4. Запросить AI-рекомендации
5. **Ожидаемо**: Митапы, конференции по менеджменту, любое время

### Сценарий 3: Стажёр без опыта

1. Войти как "Стажёр"
2. Интересы: "Обучение", "Basics", "Career"
3. График работы: "5/2 (Офис)"
4. Запросить AI-рекомендации
5. **Ожидаемо**: Обучающие мероприятия, воркшопы для начинающих

---

## ✅ Чек-лист успешного тестирования

- [ ] Консоль показывает "API ключ настроен корректно"
- [ ] Badge в Header зелёный (dev mode)
- [ ] AI-рекомендации возвращают события (2-3 штуки)
- [ ] События релевантны профилю пользователя
- [ ] При первом запуске загружаются актуальные IT-события
- [ ] Fallback работает при ошибке API
- [ ] Нет ошибок в консоли браузера
- [ ] Логи в Supabase чистые (нет 500 ошибок)

---

## 🚀 Продвинутое тестирование

### Тестирование Edge Function напрямую

```bash
# Используя curl (замените TOKEN на ваш publicAnonKey)
curl -X POST \
  https://wwxibvtflekrimlpgijo.supabase.co/functions/v1/make-server-6f7662b1/ai-recommend \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_ANON_KEY" \
  -d '{
    "prompt": "Test: Return {\"status\": \"ok\"}"
  }'
```

**Ожидаемый ответ:**
```json
{
  "choices": [
    {
      "message": {
        "content": "{\"status\": \"ok\"}"
      }
    }
  ]
}
```

### Проверка загрузки событий

```bash
# Загрузить новые события
curl -X POST \
  https://wwxibvtflekrimlpgijo.supabase.co/functions/v1/make-server-6f7662b1/seed-ai-events \
  -H "Authorization: Bearer YOUR_ANON_KEY"
```

### Проверка текущих событий

```bash
# Получить все события
curl -X GET \
  https://wwxibvtflekrimlpgijo.supabase.co/functions/v1/make-server-6f7662b1/events \
  -H "Authorization: Bearer YOUR_ANON_KEY"
```

---

## 📞 Что делать при проблемах

1. **Проверьте консоль браузера** - первым делом
2. **Проверьте Supabase Logs** - для деталей ошибок
3. **Проверьте статус badge в Header** - быстрая диагностика
4. **Прочитайте [API_KEY_CONFIGURED.md](./API_KEY_CONFIGURED.md)** - подробная информация
5. **Обратитесь к [SETUP_API_KEY.md](./SETUP_API_KEY.md)** - если нужна переустановка

---

**Статус**: ✅ Всё работает! AI-функции протестированы и готовы к использованию.
