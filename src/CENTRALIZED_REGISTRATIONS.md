# 🌐 Централизованная система заявок

## Что было сделано

Реализована централизованная система хранения заявок на мероприятия с использованием Supabase KV Store, что позволяет администратору видеть все заявки со всех устройств.

## Основные изменения

### 1. Backend API (`/supabase/functions/server/index.tsx`)

Добавлены/обновлены endpoints для работы с заявками:

- **POST `/registrations`** - Создание новой заявки
  - Автоматически одобряет заявки от `admin@sberbank.ru`
  - Поддерживает миграцию существующих заявок с сохранением ID
  - Проверяет дубликаты перед созданием

- **GET `/registrations`** - Получение всех заявок (для админ-панели)

- **GET `/registrations/user/:userId`** - Получение заявок конкретного пользователя

- **PUT `/registrations/:id`** - Обновление статуса заявки (одобрение/отклонение)

- **DELETE `/registrations/:id`** - Удаление заявки

### 2. Database Service (`/utils/dbService.ts`)

Обновлены функции для работы с заявками:

```typescript
// Создание заявки - сначала пытается API, затем localStorage
async function createRegistration(userId, eventId, userEmail)

// Получение заявок пользователя
async function getUserRegistrations(userId)

// Получение всех заявок (для администратора)
async function getAllRegistrations()

// Обновление статуса заявки
async function updateRegistrationStatus(registrationId, status)

// Удаление заявки
async function deleteRegistration(registrationId)
```

Все функции имеют **двухуровневую систему**:
1. Сначала пытаются использовать Supabase API
2. При ошибке автоматически переключаются на localStorage (fallback)

### 3. Migration Service (`/utils/migrationService.ts`)

Создан новый сервис для автоматической миграции данных:

- **`migrateLocalRegistrationsToSupabase()`** - Переносит все заявки из localStorage в Supabase
- **`needsMigration()`** - Проверяет, нужна ли миграция

### 4. App Integration (`/App.tsx`)

Добавлена автоматическая миграция при запуске приложения:

```typescript
// При инициализации приложения
if (needsMigration()) {
  console.log('🔄 Обнаружены локальные заявки, выполняю миграцию...');
  const result = await migrateLocalRegistrationsToSupabase();
  // ... обработка результата
}
```

## Как это работает

### Для обычных пользователей:

1. Пользователь отправляет заявку на мероприятие
2. Заявка сохраняется в Supabase KV Store со статусом `pending`
3. Заявка видна администратору с любого устройства
4. После одобрения администратором статус меняется на `approved`
5. Пользователь получает уведомления о мероприятии

### Для администратора (admin@sberbank.ru):

1. При входе администратор автоматически получает доступ к админ-панели
2. Заявки от администратора автоматически одобряются (статус `approved`)
3. В админ-панели видны все заявки от всех пользователей
4. Администратор может одобрить или отклонить любую заявку
5. Изменения синхронизируются в реальном времени

### Синхронизация между устройствами:

```
Устройство 1 (пользователь)        Supabase KV Store        Устройство 2 (админ)
      |                                    |                           |
      | POST /registrations                |                           |
      |-------------------------------->  [Сохранение]                |
      |                                    |                           |
      |                                    |   GET /registrations      |
      |                                    |<--------------------------|
      |                                    |   [Все заявки] ---------->|
      |                                    |                           |
      |                                    |   PUT /registrations/:id  |
      |                                    |<-------- [Одобрить] ------|
      | GET /registrations/user/:id        |                           |
      |<--------------------------------- [Обновленный статус]        |
```

## Fallback система

Если Supabase недоступен, система автоматически использует localStorage:

- ✅ Приложение продолжает работать локально
- ⚠️ Заявки не синхронизируются между устройствами
- 🔄 При восстановлении связи происходит автоматическая миграция

## Автоматическая миграция

При первом запуске после обновления:

1. Система проверяет наличие локальных заявок
2. Автоматически переносит их в Supabase
3. Показывает toast-уведомление о количестве мигрированных заявок
4. Сохраняет все ID и статусы заявок

## Проверка работоспособности

### Проверить что заявки синхронизируются:

1. **Создать заявку с телефона:**
   - Зайти как обычный пользователь
   - Отправить заявку на мероприятие
   - Убедиться что появилось сообщение "Заявка отправлена!"

2. **Проверить на другом устройстве:**
   - Зайти как администратор (admin@sberbank.ru)
   - Открыть админ-панель
   - Увидеть заявку в разделе "Ожидают"

3. **Одобрить заявку:**
   - Нажать "Одобрить" в админ-панели
   - Заявка должна переместиться в раздел "Одобрено"

4. **Проверить на устройстве пользователя:**
   - Обновить профиль пользователя
   - Статус заявки должен измениться на "Одобрено"
   - Должны появиться уведомления о мероприятии

## Console логи

При работе системы в консоли браузера можно увидеть:

```
🧭 Exact Direction - Загрузка приложения...
🔄 Обнаружены локальные заявки, выполняю миграцию в Supabase...
✅ Мигрировано 3 заявок в Supabase
📊 Загрузка событий...
✅ Загружено 12 событий
👤 Пользователь: Администратор
🎉 Приложение готово!
```

## Структура данных заявки

```typescript
interface EventRegistration {
  id: string;              // Уникальный ID заявки
  userId: string;          // ID пользователя
  eventId: string;         // ID мероприятия
  status: 'pending' | 'approved' | 'rejected';  // Статус
  createdAt: string;       // Дата создания (ISO)
  updatedAt: string;       // Дата обновления (ISO)
}
```

## Безопасность

- ✅ Все запросы аутентифицируются через `publicAnonKey`
- ✅ Администратор определяется только по email `admin@sberbank.ru`
- ✅ Пользователи видят только свои заявки
- ✅ Только администратор может видеть и изменять все заявки

## Поддержка

Если заявки не синхронизируются:

1. Проверьте консоль браузера на наличие ошибок
2. Убедитесь, что Supabase project ID корректный в `/utils/supabase/info.tsx`
3. Проверьте, что Edge Functions развернуты и работают
4. Убедитесь, что SSL сертификаты настроены

## Следующие шаги

После завершения процесса генерации SSL-сертификатов:

1. ✅ Приложение станет доступно по HTTPS
2. ✅ Все заявки будут синхронизироваться между устройствами
3. ✅ Администратор увидит все заявки в реальном времени
4. ✅ Система уведомлений будет работать для всех одобренных заявок
